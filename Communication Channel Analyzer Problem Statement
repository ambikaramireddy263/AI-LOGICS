s = "pwwkew"

l = ""
larg = ""

for i in s:

    if i not in l:
        l += i

    else:
        if len(l) > len(larg):
            larg = l

        l = l[l.index(i) + 1:] + i

if len(l) > len(larg):
    larg = l

print("Longest substring:", larg)
print("Length:", len(larg))
